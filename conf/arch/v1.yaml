# v1 Architecture: Simple CNN encoder with prototypical network
name: v1

model:
  encoder_type: conv4
  embedding_dim: 128
  conv_channels: [32, 64, 128, 256]
  distance: euclidean  # euclidean or cosine

